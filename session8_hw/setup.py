import setuptools

setuptools.setup(
    name = "session8_hw",
    version = "0.0.0",
    author = "Kamil Jaworski",
    description= "Solutions for task from session 8",
    packages = setuptools.find_packages(),
    install_requires = ['pylint', 'coverage']
)